<h1>Eat Da Burger!</h1>

<h2>Burgers are so delicious, but you need to eat one!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
    <li>{{burger_name}}
      	<button class="devoured" data-devoured="{{devoured}}" data-id="{{id}}">EAT ME!
          {{!-- this was my attempt at using partials it didn't work data-newDevoured=""="{{devoured}}"> --}}
		{{!-- {{#if devoured}}EAT ME!{{else}}WASN'T THAT YUMMY!{{/if}} --}}
	</button>

	{{!-- <button class="delete-burger" data-id="{{this.id}}">DELETE!</button> --}}
</li>
      {{!-- {{> burgers/burger-block devoured=false}} --}}

    {{/unless}}
  {{/each}}
</ul>

<h2>These burgers were so filling!</h2>

<ul>
  {{#each burgers}}
  {{#if devoured}}
    <li>
        {{!--this was my attempt at using partials it didn't work {{> burgers/burger-block devoured=true}} --}}
        {{burger_name}}
      	<button class="devoured" data-devoured="{{devoured}}" data-id="{{id}}">Wasn't That Yummy?
	</button>
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="nom">Burger Name:</label>
    <input type="text" id="nom" name="name">
  </div>

  <button type="submit">Add Burger</button>
</form>
