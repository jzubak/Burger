<h1>Eat Da Burger!</h1>

<h2>Burgers are so delicious, but you need to eat one!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>These burgers were so filling!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="nom">Burger Name:</label>
    <input type="text" id="nom" name="name">
  </div>

  <div class="form-group">
    <label for="eat">Devour?!?</label><br>
    <input type="radio" name="eat" value="1" checked> Devour!<br>
    <input type="radio" name="eat" value="0"> EATEN
  </div>

  <button type="submit">Add Burger</button>
</form>
